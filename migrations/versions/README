# Директория для файлов миграций

Эта директория содержит файлы миграций базы данных, управляемые Alembic.
Файлы создаются автоматически с помощью команды `alembic revision --autogenerate`.

Не удаляйте этот файл, он необходим для того, чтобы Git отслеживал директорию. 